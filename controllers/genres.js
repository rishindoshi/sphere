// This module is responsible for all functions that deal with music genres

//GLOBAL VARIABLE
var genreList = require('./dummy_data/dummy_data.js')();

exports.buildColorTree = function() {
  // First need to import list of genres in JSON form from external file
  // Where should we keep this file?



  return {};
}

// Randomly select N users from Ann Arbor
// Extract top25Artists from each of N users
// Extract associated genres from 25N artists
// Put all genres in a list and make_unique
// The result will be a representative sample of genres in Ann Arbor
var annArborStatistics = function() {

  return {};
}
